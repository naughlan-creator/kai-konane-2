[pytest]
testpaths = tests
# Point TEST_DATABASE_URL at Postgres to run against the engine production uses;
# unset, the suite falls back to a throwaway SQLite file so a fresh clone needs
# no database server. See tests/conftest.py.
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning:passlib.*
